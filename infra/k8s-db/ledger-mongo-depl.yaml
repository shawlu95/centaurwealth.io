apiVersion: apps/v1
kind: Deployment
metadata:
  name: ledger-mongo-depl
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ledger-mongo
  template:
    metadata:
      labels:
        app: ledger-mongo
    spec:
      containers:
        - name: ledger-mongo
          image: mongo
---
apiVersion: v1
kind: Service
metadata:
  name: ledger-mongo-srv
spec:
  selector:
    app: ledger-mongo
  ports:
    - name: db
      protocol: TCP
      port: 27017
      targetPort: 27017
